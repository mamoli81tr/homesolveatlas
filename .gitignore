# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# testing / coverage
/coverage

# env files — .env.example is intentionally NOT ignored; it has no secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# vercel
.vercel

# Claude Code local (personal) settings — .claude/settings.json (shared/team
# config, if you add one) is fine to commit; settings.local.json is per-user
.claude/settings.local.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.lighthouse/

# typescript
*.tsbuildinfo
next-env.d.ts
